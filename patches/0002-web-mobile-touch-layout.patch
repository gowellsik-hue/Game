diff --git a/Source/controls/touch/gamepad.cpp b/Source/controls/touch/gamepad.cpp
--- a/Source/controls/touch/gamepad.cpp
+++ b/Source/controls/touch/gamepad.cpp
@@
 	int directionPadSize = screenPixels / 4;
 	int padButtonSize = roundToInt(1.1F * screenPixels / 10);
 	int padButtonSpacing = inputMargin / 3;
+
+#ifdef __EMSCRIPTEN__
+	inputMargin = std::max(18, screenPixels / 18);
+	menuButtonWidth = std::max(42, screenPixels / 12);
+	directionPadSize = roundToInt(screenPixels * 0.32F);
+	padButtonSize = roundToInt(screenPixels * 0.135F);
+	padButtonSpacing = std::max(8, screenPixels / 45);
+#endif
@@
 	Circle &manaButtonArea = manaButton.area;
 	manaButtonArea.position.x = directionPad.area.position.x + (padButtonSize + padButtonSpacing) / 2;
 	manaButtonArea.position.y = directionPad.area.position.y - (directionPadSize + padButtonSize + padButtonSpacing) / 2;
 	manaButtonArea.radius = padButtonSize / 2;
+
+#ifdef __EMSCRIPTEN__
+	healthButtonArea.position.x = directionPad.area.position.x - directionPad.area.radius / 2;
+	healthButtonArea.position.y = directionPad.area.position.y - directionPad.area.radius - padButtonSize / 2;
+	manaButtonArea.position.x = directionPad.area.position.x + directionPad.area.radius / 2;
+	manaButtonArea.position.y = directionPad.area.position.y - directionPad.area.radius - padButtonSize / 2;
+#endif
